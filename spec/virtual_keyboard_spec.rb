require 'spec_helper'

describe VirtualKeyboard do
  it 'has a version number' do
    expect(VirtualKeyboard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
